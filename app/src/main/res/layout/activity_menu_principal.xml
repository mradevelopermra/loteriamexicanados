<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Botón "Cartas Loteria" -->
    <Button
        android:id="@+id/btnCartasLoteria"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginStart="80dp"
        android:layout_marginEnd="80dp"
        android:text="Baraja Loteria"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintVertical_bias="0.5" />

    <!-- Botón "Juega Loteria" -->
    <Button
        android:id="@+id/btnJuegaLoteria"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginStart="80dp"
        android:layout_marginEnd="80dp"
        android:text="Juega Loteria"
        app:layout_constraintTop_toBottomOf="@id/btnCartasLoteria"
        app:layout_constraintStart_toStartOf="@id/btnCartasLoteria"
        app:layout_constraintEnd_toEndOf="@id/btnCartasLoteria" />

</androidx.constraintlayout.widget.ConstraintLayout>
